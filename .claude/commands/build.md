Build the project for production.

```bash
npm run build
```

Run `npm run preview` afterward to verify the production build locally. Report any TypeScript errors, bundle size warnings, or failed chunks.
