# Hero Chat — Placeholder

> **Not yet implemented.** This command is a placeholder for wiring up the chat input in the Hero section.

The textarea + Send button in `src/components/sections/Hero.tsx` currently has a `// TODO: wire up chat` stub. When ready, implement:

- Connect the input to a Claude API endpoint (streaming preferred)
- Handle loading, error, and empty states
- Display the response inline or in an expanding panel below the input
- Keep the interaction above-the-fold and non-disruptive to the Hero layout

See `src/components/sections/Hero.tsx` — the `handleSubmit` function is the entry point.
