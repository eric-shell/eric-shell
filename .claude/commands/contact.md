# Contact Section — Placeholder

> **Not yet implemented.** This command is a placeholder for the planned Contact section.

When ready, implement a `Contact` page section (`src/components/sections/Contact.tsx`) with:

- Email CTA linking to `mailto:ericjshell@gmail.com`
- Links to LinkedIn, GitHub, and resume download
- A simple contact form or direct-link layout (TBD)
- Positioned above the Footer in `App.tsx`

Follow all section conventions in CLAUDE.md: `CascadeGroup`/`CascadeItem` for scroll animations, `max-w-[1440px] mx-auto px-6` container, Eyebrow + H2 header pattern.
